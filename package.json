{
  "name": "fs-tree-diff-rust",
  "version": "0.1.0",
  "description": "",
  "main": "lib/index.js",
  "author": "Tom Dale <tom@tomdale.net>",
  "license": "MIT",
  "dependencies": {
    "left-pad": "^1.1.1",
    "neon-cli": "^0.1.7"
  },
  "scripts": {
    "install": "neon build",
    "test": "neon build && mocha",
    "benchmark": "neon build && node ./benchmark.js"
  },
  "devDependencies": {
    "benchmark": "^2.1.1",
    "chai": "^3.5.0",
    "fs-tree-diff": "^0.5.2",
    "mocha": "^3.0.1"
  }
}
